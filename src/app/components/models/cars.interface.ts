export interface CarsInterface {
  _id: string;
  name: string;
  photoUrl: string;
  ownerPhone: string;
  vinCode: string;
  gosNumber: string;
  works: [string];
  worksDone: [string];
  worksInProgress: [string];

}
